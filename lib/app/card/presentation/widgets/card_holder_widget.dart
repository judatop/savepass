import 'package:atomic_design_system/atomic_design_system.dart';
import 'package:flutter/material.dart';

class CardHolderInputWidget extends StatelessWidget {
  const CardHolderInputWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdsHeadline(text: 'Holder');
  }
}
